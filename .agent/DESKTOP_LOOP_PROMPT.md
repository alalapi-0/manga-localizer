# Manga Localizer — local desktop application loop (superseded)

This prompt is **superseded** by `.agent/UI_LOOP_PROMPT.md`.

Do not re-arm `AGENT_LOOP_WAKE_manga_desktop`. Late wakes for this sentinel
skip rewrite and do not resume this loop.

The double-clickable local `.app` and package-time bundled models remain in
the tree. Further iteration is computer-use UI evaluation: operate the live
workbench as the user, write `.agent/UI_PROBLEM_REPORT.md`, and fix from
that report.

Live `/loop` paste block: see `.agent/UI_LOOP_PROMPT.md`.
