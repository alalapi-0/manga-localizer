# Manga review replacement and pending-content delivery

`ALL-PROJECTS-CODEX-GOVERNANCE-V1-MANGA-REVIEW-REPLACEMENT-LANDING-v3` — GOVERNED. Active following fresh revision Judge PASS and Governor approval; see contract_decision.json.
Read-only Governor `/root/hub_connection_governor` established this contract before mutation. V3 corrects the v2 state-format interface mismatch against the unchanged Hub validator; no attempt, Goal, repair history or cumulative budget resets. The c130b50f1584c1ecd78d9bad9a49cc9daf103937 checkpoint carries forward unchanged. Root owns control planes, registration, state, Git and installation. Bounded Repair owns only assigned product code/tests; no descendants.

## Objective and preserved authority

Deliver the original checkout's eligible uncommitted media handling, G4/G7 reopen, same-generation G8/G9/G10 replacement, safe schema migration, compact review API/UI and demand-driven loading as one coherent capability unit. Add the shared Hub2 declaration backed by the sole `.agent/STATE.yaml`, then install the accepted final candidate into the original checkout.

The original project records `OWNER_R2_REVIEW / owner-r2-review`. Preserve this fact and its owner decisions. This maintenance unit records `project_execution: not_active_in_this_governance_goal`; it neither claims the owner paused the project nor activates the original final-review Goal. Preserve 56 issues and 3 pending owner reviews from the latest verified 199-page snapshot. No page processing, automatic approval or old task revival.

## Boundaries

Stage A owns only the isolated Manga source repository based on verified main `0e2a03b27e15661b18d25c96d236577751b3391c`. The original checkout stays read-only, with HEAD `9196143d45fc29a18edcf65139134ebcdcb567e7`, index and every registered dirty/untracked preimage protected. Inventory each path as included, already delivered, superseded, protected or deferred; do not silently omit eligible product contents.

Do not read or copy `.agent/CURSOR_LOOP_PROMPT.md`; do not operate Cursor resources. Preserve Cursor-specific portions of the mixed project skill. Do not read credentials, real environment files, tokens, cookies or private keys. Do not write Hub, global Codex or Feishu, or create a second state source, scheduler, migration ledger or checkpoint system.

Never write or run application startup against real databases, images, generated artifacts, verdicts, revisions, queues or lineage. No real API, worker, Vite, uvicorn, provider, paid service or new image generation. Narrow read-only evidence for pages134/167/192 is allowed; derived calculations/conversions belong only in disposable temporary fixtures, never imported into the real project. Preserve all immutable originals and historical failed candidates. No force push, protection bypass, reset, clean, stash, history rewrite or whole-branch import.

## Serial delivery

1. Register original preimages/modes/dispositions and verify scope, runtime routes and real remote default branch. Reuse unchanged delivered evidence.
2. After content/secret review, make a local STATE-only checkpoint commit preserving exact eligible original `.agent/STATE.md` bytes. Keep its historical failure details only in Git, without a second state file.
3. Root adds strict alias-free `.agent/STATE.yaml` as the sole current authority, retires `.agent/STATE.md` from the final tree, and updates active Agent, shared skill and documentation routes. Preserve the old Markdown state only in checkpoint Git history; no compatibility shell or duplicate authority. Copy only inventoried product files and necessary shared skill hunks; repair the coherent candidate in isolation.
4. The final candidate and the entire base-to-head series, including the STATE checkpoint, receive full applicable checks and `npm run audit:ci -- --base 0e2a03b27e15661b18d25c96d236577751b3391c --head <full-candidate-sha>`. Root may create local candidate commits for this audit. Product release remains unapproved and its stricter historical path checks are not waived.
5. Root registers exact semantic identity, preimages, commands/exits and original preservation. Fresh read-only Judge reviews the whole series; Governor decides. Only an approved exact candidate is normally pushed to real main, followed by remote ancestry and CI verification. Relevant drift/content changes require new registration/review.
6. Stage B begins only after source delivery/CI. Recheck original HEAD/index/status/preimages and install accepted bytes by exact per-file/hunk CAS, adding the YAML state and retiring the exact-preimage Markdown state while preserving non-owned content. Original Git HEAD/branch/index remain unchanged. Validate only synthetic databases/media/API and an isolated real browser flow. Register a separate installation candidate and inverse transformations, obtain fresh Judge and Governor, and normally deliver its receipt through source main with CI. No live migration occurs in either stage.

## Acceptance

1. Every original uncommitted path has a unique disposition; already delivered identical files are reused; no eligible product content or uncommitted history is silently lost from the reviewed series.
2. The sole `.agent/STATE.yaml` preserves OWNER_R2_REVIEW, owner decisions, pending work, current blockers/evidence and one next action. AGENTS identifies its strict YAML format. The Markdown file is absent from the final tree. Searches/tests prove all active readers route to YAML and no machine consumer still requires Markdown heading/label parsing. Historical path references are explicitly historical. Static documents cannot activate the project Goal.
3. `hub.connection.yaml` uses the same path, `format: yaml`, typed path selectors and all 21 known/unknown field semantics, including a nonempty fallback reason for every mapped or unmapped field. Dynamic counts, blockers and receipts live once in STATE; no value-map copy of dynamic business truth. Completed, accepted, delivery and governance execution remain distinct, with truthful199-page counting basis.
4. Hub's unchanged shared validator validates the declaration and isolated source read through a disposable per-ID registry fixture, without reading other project files, executing validation_entry, writing a Hub ledger or accessing excluded/credential roots.
5. JPEG/PNG/WebP are validated from actual bytes and explicit MIME; unsupported/corrupt/lying MIME inputs fail closed. Raw bytes remain immutable; deterministic normalized output, orientation, dimensions and extension handling remain honest across upload, thumbnail and candidate consumers.
6. Whole-frame normalization/registration binds raw/source grids, crop/fit parameters and digests. Invalid geometry, visual holes/residual text and mask-outside changes cannot become accepted results. Preserve all existing quality checks and opt-in transform bounds.
7. Page134 raw87f1b497… and failed normalized95423794… remain immutable evidence. The measured long-vowel ROI(2015,480)-(2065,795) contains3339 dark source pixels outside accepted G7 mask17d30eeb…, all retained by strict composite with zero outside-mask changes. The accepted G4 quality box is (1920,50)-(2120,550); 2474 of those 3339 pixels lie below its lower edge. Treat this as an upstream G4/G7 coverage failure requiring legal G4 reopening, downstream revalidation and a new accepted G7 mask; do not weaken strict composite or manufacture/relabel a new raw. Actual page correction/import remains a later reviewed effect.
8. Legal exact G4/G7 reopen invalidates only actual downstream dependencies, preserving old masks/candidates/reviews/revisions. Cover a missed lower-edge long-vowel mark, edge glyphs, halo/support expansion, G4 revision CAS, mandatory G5/G6 revalidation, G7 reacceptance and exact downstream invalidation with synthetic fixtures. G8 visual failure preserves valid G0–G7; G10 failure preserves valid G8; source restart requires evidenced source/quality/mask invalidity.
9. Same-generation G9 terminals bind the new G8 checksum and G10 binds the new G9 terminal; retain every prior candidate, failure, review, terminal and revision. All mutations use applicable revision/generation/parent CAS. Stale/duplicate requests cannot cause partial writes, duplicate terminals or overwrite newer decisions.
10. Constraint migration is semantic-schema-detected, one-time, idempotent, transactional and rollback-safe. No unconditional new index/trigger drops or review table rebuild on every open. Preserve rows/IDs/timestamps/JSON/revision/FK relations, append-only and validation triggers, checks and unrelated indexes. Include both old inline unique constraints, not only named indexes.
11. Isolated empty/current/nonempty legacy/renamed-constraint/failure fixtures prove integrity_check, foreign_key_check, exact row hashes and registered schema object changes, failure rollback/retry, protection behavior and zero migration DDL on repeated migrated opens. Real databases are not used as fixtures or migrated.
12. Compact API keeps required stable order/revision and truthful errors; awaited asyncio.to_thread remains synchronous HTTP completion, with responsiveness, CAS, idempotent durable results and failure behavior. No new background-job/query subsystem.
13. Existing review actions retain pending/success/error, duplicate-click protection, recoverable failures and stale-response handling. Preserve prior retry-handoff, audit, G4–G10 and UI test intent. Demonstrate appropriate browser interaction/persistence/error/console/network behavior on synthetic data.
14. Project opening and single-page selection load current/visible page work rather than every page's generations/events, with explicit on-demand refresh and request-count tests.
15. Full backend, frontend, relevant E2E, migration failure injection, browser review-flow, state/Hub declaration validation, diff check and normal CI audit pass. Root reproduces material checks. Each stage gets one fresh exact-candidate Judge and Governor decision; no overall/page-generation completion claim.

## Later Hub routing unit

The real Hub registry and ledger remain untouched during this Manga unit. Only after Manga source delivery and original installation are accepted may a separate serial Hub unit change the Manga registry route from `.agent/STATE.md` to `.agent/STATE.yaml` and perform the real refresh. Until then, report declaration validation/delivery separately from pending Hub integration; no successful live connection claim.

## Recovery

Forward repair with unchanged cumulative Goal history. Roll back only owned exact fixture/registered preimages after drift checks; no real-database experiments. An original-install conflict blocks only its dependent hunk group. Persist exact failures and one recovery action; never equate partial delivery or missing authority with completed acceptance.
