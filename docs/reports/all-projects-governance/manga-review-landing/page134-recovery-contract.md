# Page 134 controlled recovery

`ALL-PROJECTS-CODEX-GOVERNANCE-V1-MANGA-PAGE134-CONTROLLED-RECOVERY-v1` — GOVERNED. Read-only Governor `/root/hub_connection_governor` established this contract before mutation. This is a bounded unit of the existing owner-authorized all-projects Goal, with unchanged cumulative budget, failure history and health checkpoints. It does not revive the historical 199-page task.

## Objective and exact target

Correct the demonstrated G4/G7 coverage defect and complete the supported G4 → G5 → G6 → G7 → new native G8 → G9 → G10 → final-review refresh chain for Page134. A successful result becomes strict pending; only the owner may approve it. All-project and corpus completion remain separate.

- Project: `b1b85f3e-4d72-4956-b519-6b30fee02fcc`.
- Item: `7975117b-c953-4ac4-847e-5249d9521a2c`.
- Repair image: `71a0fad1-5fb2-4e14-8bd8-7624f1cd2cc9`.
- Generation: `809e3762-7d11-40f8-88cf-99f6d9111691`.
- Run: `final-review-7975117b-r2-a2`.

Historical image r50, region r4 and next63 are evidence only. Read current revisions, lineage and parent checksums before every effect. The diagnosed long-vowel ROI has3339 dark pixels outside accepted G7,2474 below the old G4 lower edge. A materially corrected G4 and downstream revalidation are a narrow recovery from this new evidence, not blanket reopening or an unchanged generation retry. Preserve the old accepted mask and failed raw candidates as immutable history; never relabel old bytes as new output.

## Authority and protected boundaries

The current owner Goal authorizes actual failed-case recovery and normal main delivery. The owner's existing native image route authorizes the necessary page/quality/mask upload and one built-in `image_gen` generation through native `cloud:image`; it does not authorize a new provider, paid API, credentials, purchases or account/model changes. Static files and historical tasks never grant runtime authority.

Root alone owns control planes, registration, state, real data effects, generation and Git. Repair may own only specifically assigned source code/tests. Original checkout HEAD `9196143d45fc29a18edcf65139134ebcdcb567e7`, index `df48fe3a5e945aa4c4a01bc2dcacc8b3004bd3672431b63b190e05991d536b99`, branch and all registered pending files remain protected. No original Git operation or whole-branch import. Source starts at verified clean main `1aa3fb8a80241bf7a533b13eeb08771a428f4839`.

Preserve G0–G3, original source, historical raw/masks/candidates/reviews/terminals/revisions, all other198 pages, all140 approved items, owner-pending112/24/173, pages167/192 and remaining issues. No source restart, automatic approval, corpus export, storage migration/cleanup, unplanned database/media mutation, default all-project worker, unrelated catalog/queue recovery, `/projects/open` recovery, Cursor, secrets, real environment configuration, Hub/Feishu or sibling repository effects. Real databases, images and verdicts stay out of Git. No force push, reset, clean, stash or history rewrite.

## Required capabilities

Provide a fail-closed single-project API-only entry bound explicitly to the selected project, catalog, database and media root. Worker-disabled startup must not start a worker, recover jobs or consume queues. A dormant PersistentJobQueue service object may reuse the exact-item execute core; it must never start a task/thread, claim/poll loop or background recovery. Startup/open must not load, normalize, migrate or delete unrelated catalog/media, and recognition/schema checks must not delete existing generated media. Predict and register the exact schema/table/file effect set on an isolated clone before any live open. Unexpected schema, normalization, deletion or queue effects stop the route. Native CLI uses this restricted local HTTP entry.

The sole STATE and validator must represent owner-authorized single-page recovery while current business state stays `OWNER_R2_REVIEW / owner-r2-review`. An active recovery declaration requires the current contract, exact target, activation reference and single-page limits; it remains data, not an authorization grant. Closing or stopping this bounded unit restores `project_execution: not_active_in_this_governance_goal` and retains the actual next action.

## Serial stages and acceptance

1. **A — source prerequisites.** Root implements control/state/validator changes; bounded Repair may implement necessary startup and queue isolation. Use synthetic database/media/multiple-catalog fixtures to prove exact target isolation, safe startup, fail-closed inputs, unchanged protected rows/files, duplicate/stale request behavior and existing gate behavior. Run backend/frontend/relevant E2E/state/storage and complete normal CI audit. Register the exact source candidate and preimages; fresh Judge PASS and Governor decision precede normal main push, remote ancestry and CI verification.
2. **B — installation and clone proof.** After A delivery, register original source/state and coherent database/WAL/SHM, selected rows, media, lineage and current CAS preimages. Install exact approved source hunks by CAS. Use SQLite consistent backup and independently copied Page134 media for an isolated clone, with no links to mutable real media. Exercise the complete G4–G10/refresh flow using explicitly synthetic G8 evidence. Prove the exact startup/migration/logical-row/media effect set, integrity/FK checks, protected schema objects and non-target hashes. Register installation plus execution plan; fresh Judge and Governor must accept before real database/media effects.
3. **C — actual single-page effects.** One Root process and one page, serial CAS-bound operations. Legally reopen G4, verify actual glyph/halo geometry rather than guessed dimensions, and revalidate G5/G6. When changed geometry invalidates OCR, only the existing guarded local OCR engine may recompute this page, without downloads, new providers, network, fees, credentials or fallback; register input/result checksums, engine, current lineage/CAS and actual call count. Produce a new G7 through the supported mask path and retain actual coverage, halo/support, eligible-region, geometry binding and outside-mask collateral checks. After accepting that mask, use the authorized native image tool once. Ingest the new raw once through native `cloud:image`, with exact MIME/dimensions/checksum/generation/parent identity. Preserve raw immutability and all ten existing ordered G8 visual checks, including residual text, white/gray holes, continuity and zero outside-mask changes. Failure preserves a specific blocker and legal re-entry point; do not generate again or advance into G9. Success binds G9 to new G8 and G10 to new G9; unchanged reviewed translation content may be reused with new bindings, without a translation-provider call. Refresh only the target item into strict pending. Stop the restricted server and leave no worker/queue or other-page effect.
4. **D — effect review and receipt.** Register exact postimages, logical row deltas, media hashes, API results, lineage, actual batch counts and non-target invariants. Fresh read-only Judge reviews the actual candidate; Governor decides. Deliver only sanitized STATE/contract/concise evidence through source main with normal audit and CI. Original Git stays unchanged. Hub refresh is a later independent unit.

Governor clarified these G7/G8, existing-local-OCR and inert-service-object wordings in the same native contract; no frozen authority, target, effect, budget or stage changed. Initial and final project queues must remain empty; duplicate/stale exact requests cannot create additional jobs or partial writes.

The expected counting basis remains the observed140 approved/56issues/3pending until actual refreshed catalog evidence changes it; never prefill an expected outcome. Source/code acceptance, clone acceptance, actual gate quality, owner approval and remote delivery remain distinct. Synthetic images cannot prove real generation or visual success.

## Identity and recovery

Use the existing evidence directory and one `page134-recovery.json` registration. Freeze actual semantic code/state/effect identities and reproducible checks without hashing self-containing records. Explicit decision, delivery and next-action metadata stays outside semantic candidate identity; changed behavior/effects require fresh appropriate review. No new scheduler, state authority, migration ledger or duplicate progress system.

Prefer forward repair with retained failures. A real failure stops at the last confirmed gate with current CAS and one legal next action. Only registered target rows/new media with exact postimage CAS may be reversed; never restore an entire database, directory or checkout over other work. Verify uncertain external generation outcomes through the existing request/output before considering any subsequent action.
