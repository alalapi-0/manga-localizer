# Page 134 controlled recovery v2

`ALL-PROJECTS-CODEX-GOVERNANCE-V1-MANGA-PAGE134-CONTROLLED-RECOVERY-v2` — GOVERNED. Governor `/root/hub_connection_governor` supplied this inactive revision and its native clarifications. Root registers the exact draft after v1 Stage D remote/CI verification; a fresh independent Judge and Governor must accept the revision before it becomes effective. An effective contract is not execution activation: A/B remain inactive, C requires its accepted exact plan. Decision metadata lives in the existing registration, outside this content identity.

## Objective, lineage and target

Use the demonstrated remaining left glyph/white-halo/internal-hole defect to make a materially different G7, then attempt one new native G8. Only if all quality gates pass, bind G9/G10 and refresh this item to strict pending. Only the owner may approve it. This is the same all-projects Goal, not the historical whole-book task. Preserve cumulative budget, failure history, completed first250k rebaseline and health checkpoints without reset.

Exact target: page134; project `b1b85f3e-4d72-4956-b519-6b30fee02fcc`; batch `a734c596-faae-4875-ae61-f694a3c26d4a`; item `7975117b-c953-4ac4-847e-5249d9521a2c`; image `71a0fad1-5fb2-4e14-8bd8-7624f1cd2cc9`; generation `809e3762-7d11-40f8-88cf-99f6d9111691`; run `final-review-7975117b-r2-a2`. Post-v1 baseline: project9267/image73/next92/item2/batch485, queues0. Reread current CAS before every actual effect; drift stops that dependent route for new evidence/registration.

Prior v1 receipt remains in Git commit `1cb5a7ae3877c283c88fb1f43727652dee7ea458` at `docs/reports/all-projects-governance/manga-review-landing/page134-recovery.json`, semantic SHA `d1c93d67aa28a4b37a8a5bb9ddd5373905b17cb1eccff5ee0cf2c0848c9ade41`. Candidate `eeda45ad7cb658e7aecab89d468425662c085898` is an ancestor of that verified main; CI34249787776 succeeded in backend/frontend/E2E. Preserve this immutable history rather than copy another ledger.

## Authority and protection

The owner's existing Goal permits bounded actual recovery, the existing native image route and normal main delivery. It does not authorize new providers, paid APIs, credentials/account/model changes or publication. Preserve G0–G6, all20 regions and reviewed source, the v1 right long-vowel fix, every old mask/raw/candidate/review/revision/job and earlier a1/a2 failure history. Preserve other198 pages, all140 approved, owner-pending112/24/173, pages167/192, original source and all non-target data. No G4/G5/G6/OCR effects in v2; no translation-provider call, Gemini/LaMa/new model or fallback.

Root owns control planes, state, evidence, real effects, native generation and Git. Repair may write only specifically assigned source code/tests. Original HEAD `9196143d45fc29a18edcf65139134ebcdcb567e7`, branch/index and registered existing pending work remain protected; no original Git mutation or whole-branch import. Installation uses exact per-file/hunk CAS only after source acceptance. No Cursor, secrets, real environment configuration, other repositories, Hub/Feishu refresh, corpus export, storage migration/cleanup, owner approval, default worker, queue claim/poll/recovery or unrelated catalog startup. No force push, reset, clean, stash or history rewrite. Native generation stays serial through the existing restricted loopback API and native ingest; stop its exact Root process at closure.

## Exact profile and cumulative effects

Active declaration must explicitly select `page134_g7_material_remask_v2`, bind this exact contract, complete target, current registration and independently accepted execution plan. Its exact six-effect set is `g7_mask`, `g8_native_generation`, `g8_native_ingest`, `g9_bind`, `g10_typeset`, `final_review_refresh`. No arbitrary subset, extra effect, v1 mixing or other target. Retain one page slot, native generation limit1, ingest concurrency1, and false automatic-owner-approval/export/background-worker limits.

For this v1/v2 controlled-recovery series only, declare exact integer maps: `prior: {generation: 1, ingest: 1}`, `additional: {generation: 1, ingest: 1}`, `cumulative: {generation: 2, ingest: 2}`. Bind the prior receipt's full Git commit/path/semantic SHA and exact target. Each prior plus additional must equal cumulative; current consumed counts and registration cannot contradict or reset the series. v2 allows at most one additional native generation and one ingest. This does not claim only two generations in all Page134 history; earlier a1/a2 artifacts remain intact.

The validator only checks static declarations: exact profile/contract/target/receipt/registration, keys, integer types, counts, arithmetic and exhausted/inconsistent active declarations. It never intercepts actual image_gen or creates runtime authority. Root checks v1 Git bytes/ancestry and actual native/ingest records at A/B/D. No new history copy, validator network/Git calls, general quota executor, scheduler or second counter system. Renaming a task/contract/request/agent/session cannot reset spent calls.

## Material G7 and quality

Use the existing text recipe plus registered precise add strokes to cover complete left glyphs, white halos and internal holes. Retain v1 right coverage and all existing support; no support removal. A text polygon is only a segmentation ROI, not a filled region; do not broadly enlarge a region rectangle or use feather as alpha repair. New support must be evidenced against source/context. Protect existing unoccluded art; where target text/halo/holes occupy clothing or background, covering that occlusion to reconstruct behind it is allowed. Do not swallow unrelated character or background detail.

The v1 raw LANCZOS/strict recomposition mismatch0 and three unmasked ROI best shifts0/0 are reusable diagnosis evidence, not predictions of future provider output. New G7 must pass actual coverage, geometry/eligible-region binding, halo/support and collateral checks. New G8 is judged from its actual immutable raw, normalized image and strict composite using all ten existing ordered checks. Zero outside-mask changes alone never implies visual acceptance. No old failed raw reuse or source restart.

## Serial stages and acceptance

1. **A — source prerequisites.** Root minimally implements the named profile and count binding with meaningful positive/negative state tests. STATE stays inactive. Run relevant state/backend checks, frontend and relevant E2E checks; reuse valid unchanged evidence with exact applicability and run complete normal CI/audit. Freeze source, preimages and evidence; fresh Judge PASS and Governor approval precede normal source main delivery, remote ancestry and all applicable CI success.
2. **B — install and clone.** After A delivery, register current original source/state and coherent DB/WAL/SHM, schemas, selected rows, lineage and media preimages. Install accepted hunks by CAS. Create a consistent SQLite clone with independently copied media, no mutable-real-media links. Prove the existing owner-issues-remask route opens because this exact linked item is still issues, then G7 → explicitly synthetic G8 → G9 → G10 → strict pending. Prove G0–G6/20 regions/source/old history and non-target rows/media unchanged, no OCR/G4–G6 jobs, exact support/ROI/collateral and row/media effects, schema/FK/trigger/index integrity, no duplicate or stale-request side effects. Fresh Judge and Governor accept the exact installed/clone/execution-plan candidate before real effects.
3. **C — actual page.** Recheck CAS/profile/prior/current counts and execute only accepted G7. After formal G7 acceptance, use exactly one additional native image_gen and one native ingest with exact checksums/parents. Evaluate all ten ordered G8 checks. Any failure is formally rejected with raw/mask/normalized/review retained; stop immediately, no G9/G10/refresh or second generation. Only an all-pass G8 permits G9 bound to new G8, G10 bound to new G9 and target-only strict-pending refresh. Reuse reviewed translations with new bindings, no provider. No owner approval.
4. **D — actual receipt.** Register actual postimages, native and ingest counts including v1, row/media deltas, visual assessments, CAS, queues and non-target invariants. Fresh Judge reviews exact actual evidence, then Governor decides. Normal source-main delivery includes only sanitized STATE/contract/compact receipt, with ancestry and CI verified. Original Git stays unchanged; Hub is a separate later unit.

Quality failure leaves actual140/56/3; successful target refresh must prove actual140/55/4 from all199 rows. No mock progress. Final queues0 and no worker, OCR, cross-page/provider fallback/approval/export effects. Failed-at-G8 receipt acceptance is distinct from image success, owner acceptance, corpus completion and all-project Goal completion. A semantic change receives fresh appropriate review; pure decisions/delivery/next-action metadata do not reopen unchanged evidence.

## Evidence and recovery

Reuse this contract path and the single `page134-recovery.json` registration. Retire v1 detail through its exact Git pointer, retaining known failures and review differences, source checks, installation and actual artifacts. Keep compact current state, exact candidate and reproducible evidence, one next action and cumulative telemetry; no duplicate per-round ledger.

At failure stop at the last confirmed gate, preserve current CAS and cumulative use, name the precise cause and legal resume condition. Only registered target new rows/media with postimage CAS may be reversed; never restore an entire DB/directory/checkout. Verify an uncertain native result through existing request/output before considering further action; this contract does not permit another generation after its additional call is spent.
