# Frontend requests are relative to /api by default. Set this only for split hosting.
VITE_API_BASE=/api

# Vite dev proxy target; credentials must remain in the backend/session environment.
VITE_DEV_API_TARGET=http://127.0.0.1:8000
